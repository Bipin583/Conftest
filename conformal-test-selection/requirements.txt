# Conformal Test Selection - runtime dependencies
#
# NOTE ON PINS: these are the versions the project was specified against.
# The development machine currently resolves newer releases (xgboost 3.2.0,
# scikit-learn 1.9.0, pandas 2.2.0, flask 3.1.3). All source in this package is
# written against the intersection of both APIs, so it runs unmodified under
# either set. See README.md -> "Environment compatibility".

# --- Gradient boosting / ML core ---
xgboost==1.7.5
scikit-learn==1.3.0
mlxtend==0.23.0
pandas==2.0.0
numpy==1.24.0
scipy==1.10.1

# --- Serving ---
flask==2.3.0

# --- Data collection ---
pygithub==1.59.0

# --- Infrastructure ---
pyyaml==6.0
tqdm==4.65.0
joblib==1.3.0

# --- Testing ---
pytest==7.4.0
